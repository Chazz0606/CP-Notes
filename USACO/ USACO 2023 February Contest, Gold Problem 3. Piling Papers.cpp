#include<bits/stdc++.h>
using namespace std;

const int N = 302;
const int MOD = 1e9+7;
int paper[N] = {};
map<string, int> record[3] = {};
map<string, int> dp[N][N] = {};

int main() {
    int n; long long a, b; cin >> n >> a >> b;
    string as, bs;
    for (int i = 1; i <= n; i++) cin >> paper[i];
    for (int i = 0; i < as.length(); i++){

    }


    int q; cin >> q;
    for (int i = 0; i < q; i++) {
        int l, r; cin >> l >> r;

    }

    return 0;
}